package com.java.collection;

public class CollectionTheory {

	/*
	 * Collection is a framework that provides an architecture to store and
	 * manipulate the group of objects. it can achieve all operations that is
	 * performed on a data such as searching, sorting, insertion, manipulation,
	 * deletion.
	 * it is a single unit of objects.
	 * it has interfaces, implementations and algorithm.
	 
	 * hierarchy of Collection
	 		* Iterable (interface)
	 			* Collection (interface)
	 				* List (interface)
	 						* ArrayList (class)
	 						* LinkedList (class)
	 						* Vector (class)
	 						* Stack (class)
	 				* Queue (interface)
	 							* PriorityQueue (class)
	 						* Dequeue (interface)
	 				* Set (interface)
	 								* HashSet (class)
	 								* LinkedHashSet (class)
	 						* SortedSet (interface)
	 								* TreeSet (class)
	 	*/
	
}
